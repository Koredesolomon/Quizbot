services:
  mongo:
    image: mongo:7
    container_name: quiz-bot-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - quiz_bot_mongo_data:/data/db

volumes:
  quiz_bot_mongo_data:
